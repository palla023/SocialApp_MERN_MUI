import { Box } from "@mui/material";
import { styled } from "@mui/system";  // used for styling components with custom CSS.

const FlexBetween = styled(Box)({
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center",
});

export default FlexBetween;


